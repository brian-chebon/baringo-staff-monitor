# Copy to .env and fill in. Anything in .env stays out of version control.
# Currently the app does not read a .env file; this is a placeholder for
# future build-time configuration.

GOOGLE_MAPS_ANDROID_API_KEY=
GOOGLE_MAPS_IOS_API_KEY=
